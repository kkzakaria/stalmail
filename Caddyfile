:443 {
  tls internal
  # Explicit IPv4 loopback (not "localhost") so the upstream never resolves to ::1
  # while the app binds 0.0.0.0 — the mismatch otherwise yields a 502.
  reverse_proxy 127.0.0.1:3000
}

:80 {
  redir https://{host}{uri} 308
}
